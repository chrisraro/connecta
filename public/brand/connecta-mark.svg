<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" role="img" aria-label="Connecta">
  <title>Connecta</title>
  <!--
    PLACEHOLDER ARTWORK — this is not the final mark.

    The geometry below (a solid Greek sigma) is carried over unchanged from
    the product's previous identity. It has not been replaced with
    Connecta's own lettermark yet — treat this as provisional, not a
    finished design decision. See components/brand/ConnectaMark.tsx, which
    carries the same warning on the inline-delivery version of this
    artwork, and marketing/README.md for how this file is used.

    Drawn in currentColor so it inherits from context and needs no network
    request when inlined.
  -->
  <path
    fill="currentColor"
    fill-rule="evenodd"
    d="M16 10 H50 V18 H32 L44 30 V34 L32 46 H50 V54 H16 V46 L30 32 L16 18 Z"
  />
</svg>
